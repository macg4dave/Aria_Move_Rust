# Fail the job on vulnerabilities.
# Keep “yanked” as informational (don’t fail the build).

[advisories]
# Show some informational warnings but fail on real advisories.
informational_warnings = ["unmaintained", "unsound"]
ignore = []  # add specific RUSTSEC-* IDs here if needed

[output]
quiet = false
json = false