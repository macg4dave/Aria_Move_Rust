# Fail the job on vulnerabilities.
# Keep “yanked” as informational (don’t fail build).
[advisories]
# Explicitly list which informational warnings to show;
# omit "yanked" so it won’t fail the build.
informational_warnings = ["unmaintained", "unsound"]
ignore = []  # add specific RUSTSEC-* IDs here if needed

[output]
quiet = false
json = false
```# filepath: /Users/dave/github/aria_move_rust/Aria_Move_Rust/.cargo/audit.toml
# Fail the job on vulnerabilities.
# Keep “yanked” as informational (don’t fail build).
[advisories]
# Explicitly list which informational warnings to show;
# omit "yanked" so it won’t fail the build.
informational_warnings = ["unmaintained", "unsound"]
ignore = []  # add specific RUSTSEC-* IDs here if needed

[output]
quiet = false
json = false