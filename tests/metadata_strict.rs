// Strict metadata mode removed; this file is retained to avoid rename churn.
// Keep a placeholder test to ensure the suite runs.

#[test]
fn metadata_strict_removed_placeholder() {
    assert!(true);
}
